import { StyleSheet } from "react-native";

import constants from "../../config/constants";

export default StyleSheet.create({
  container: {
    paddingVertical: constants.paddingSmall * 0.8,
    width: "100%"
  }
});
